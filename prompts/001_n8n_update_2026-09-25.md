# VERDICT Update Evaluation Prompt — #001 n8n (update, 2026-09-25)

Framework v0.3.2 Differential Evaluation. Interrupt lane (T1 ×n, T2 already on record, T5 candidate) with folded routine scope. Fifth in the LayerC-Triage-001 order (pilot 5). Drafted from the 2026-09-22 sweep dry run, which observed more than 100 GitHub Security Advisories published against `n8n` (npm) in the trailing 12 months (listing capped at `per_page` = 100; pagination not performed), while the record carries `cve_count_12mo: 12` with basis `lower_bound` and one KEV entry.

---

## RECORD UNDER UPDATE

**Name / slug:** n8n / n8n
**Evaluation number:** 001 (unchanged)
**Current canonical record:** `platforms/n8n.md` at ZinovaCreation/verdict-platforms `31152b1` (or later) — read in full before starting. It is a migrated capture: body carries captured sentences ("12+ CVEs in trailing 12 months. Two CVSS 10.0 vulnerabilities confirmed (CVE-2026-21858, CVE-2026-21877). CVE-2025-68613 added to CISA KEV catalog March 2026 — FCEB patch deadline March 25, 2026. Blocklist-based sandbox bypass confirmed across 5+ independent CVEs.") plus fragmentary Scorecard / Incident Timeline / Contextual Analysis lines captured verbatim for the KEV protocol (RevisionCohort-001), `qa` unresolved ×3, `evaluator_model: unrecorded`, `sources` absent (0 URLs), `homepage: null`, `github: null`, `target_version: null`, `tags: []`, `operator: Independent · n8n GmbH (Berlin)`, `independence: unrecorded`, `parent_entity: null`. Only R is marked `re-evaluated` in `differential` (the 2026-03-24 update); V/D/I/C/T are `null`.
**evaluated_at (never changes):** 2026-03-13
**Previous evaluation (→ `previous_evaluation_date` / `previous_score`):** 2026-03-24 / 35
**Evaluation History so far:** 2026-03-13 Initial (Layer 0) 40 / Tier C / v0.3.1 · 2026-03-24 Update 35 / Tier C / v0.3.1 (provenance correction 2026-09-22 per StrategyApproval-001 clause (c); Evaluation History table already present in the body)
**Prior framework version / evaluator model:** v0.3.1 / unrecorded
**Prior R fields:** `cve_count_12mo: 12` (basis `lower_bound`), `max_cvss_12mo: 10.0`, KEV entries: CVE-2025-68613 (`kev_added_date` 2026-03-11, deadline 2026-03-25), `supply_chain_compromise_12mo: false`; R 5/20 (V 12 · D 3 · I 6 · C 3 · T 6)
**Prior record type:** migrated capture without `sources` (KEV protocol sentences captured verbatim)

---

## TRIGGER

Facts and sources only; the engine verifies each on a primary source before use. The list is representative, not the count.

| Trigger | Fact | Date | Source URL (engine verifies on the primary source) |
|---|---|---|---|
| T2 (on record) | CVE-2025-68613 ("n8n Improper Control of Dynamically-Managed Code Resources Vulnerability", CWE-913) in the CISA KEV catalog: dateAdded 2026-03-11, dueDate 2026-03-25, knownRansomwareCampaignUse Unknown (catalog 2026.09.24, 1,723 entries). Inside the window; the full KEV protocol applies again (Scorecard flag, Incident Timeline, Executive Summary, Contextual Analysis; listing date, FCEB deadline, elapsed time from initial disclosure to KEV addition). Engine checks the current KEV JSON for any additional n8n entry. | added 2026-03-11 | https://github.com/cisagov/kev-data (known_exploited_vulnerabilities.json) ; https://www.cisa.gov/known-exploited-vulnerabilities-catalog ; https://nvd.nist.gov/vuln/detail/CVE-2025-68613 |
| T1 | 2026-07-22 advisory batch published by the maintainers (csuermann): GHSA-xwx6-jjhv-84p8 (prototype pollution via dot-notation field names → instance-wide DoS, High), GHSA-xmc9-4f2h-jf9c (Edit Image node format injection → arbitrary file write, High), GHSA-rcv6-pvrj-4xcg (authenticated code execution in the Git node, High), GHSA-2x35-3fw4-9jr4 (High), GHSA-cj9h-qx8g-pq2g (shared-workflow editor credential exfiltration via inline sub-workflow JSON), GHSA-652q-gvq3-74qv (Snowflake node SQL injection, Moderate), GHSA-vhf8-cg2h-cg3p (SSRF protection bypass via MCP Client node, Moderate), GHSA-hx4h-vr3m-45vh (prototype pollution via VM expression engine sandbox escape → DoS, Moderate). Engine reads CVE IDs, CVSS 4.0 (n8n's primary score) and 3.1 vectors, affected and patched versions for each. | 2026-07-22 | https://github.com/n8n-io/n8n/security/advisories |
| T1 | GHSA-jjpj-p2wh-qf23: JavaScript Task Runner sandbox (`@n8n/task-runner`) vulnerability; fixed in n8n 1.123.22 / 2.9.3 / 2.10.1 (task-runner 1.59.16 / 2.9.1 / 2.10.1 per the advisory-database improvement PR). Attribution note: the vulnerable code ships in the scoped package `@n8n/task-runner`, which external Task Runners run standalone — engine treats it as product-scoped (operator's own package) and records the sub-package. | 2026-07 → 08 (engine to read) | https://github.com/n8n-io/n8n/security/advisories/GHSA-jjpj-p2wh-qf23 ; https://github.com/github/advisory-database/pull/8971 |
| T1 | GHSA-597w-c3jh-g8fg: High; published 2026-09-16 (repository advisory, published by Matsuuu). Most recent advisory at drafting — engine reads title, CVE ID, CVSS, affected / patched versions. | 2026-09-16 | https://github.com/n8n-io/n8n/security/advisories/GHSA-597w-c3jh-g8fg |
| T1 (in window, pre-dating the 2026-03-24 update) | CVE-2026-21858 (GHSA-v4pr-fm98-w9pg, unauthenticated file access via webhook request handling, CVSS 3.1 10.0, fixed 1.121.0, published 2026-01-07), CVE-2026-21877 (GHSA-v364-rw7m-3263, RCE via arbitrary file write / Git node, 9.9 GitHub CVSS 3.1 (10.0 in the prior record — engine reconciles the score source), fixed 1.121.3, 2026-01-06), CVE-2025-68668 (GHSA-62r4-hw23-cc8v, arbitrary command execution via Code node Python, 9.9), CVE-2025-68613 (GHSA-v98v-ff95-f3cp, RCE via expression injection, fixed 1.122.0, 2025-12-19; later KEV), CVE-2026-25049 (GHSA-6cqr-8cfr-67f8, expression escape → RCE, 9.4, 2026-02-04). Engine confirms publication dates against the window and reconciles with the prior `cve_count_12mo: 12 (lower_bound)`. | 2025-12 → 2026-02 | https://github.com/n8n-io/n8n/security/advisories ; https://github.com/advisories?query=n8n |
| T5 (candidate) | Ownership / capital structure events inside the window: USD 180M Series C (2025-10-09) led by Accel with NVentures (NVIDIA) and T.Capital as corporate investors; a strategic investment from SAP reported in May 2026 (USD 70M at ~USD 5.2B per secondary sources). Neither is an operator change on the public record; engine verifies on n8n's own announcements whether control changed (`independence` stays `independent` unless a controlling stake is disclosed) and evaluates the disclosure layer (Special Consideration 3). | 2025-10-09 · 2026-05 | https://blog.n8n.io/ (Series C announcement) ; n8n / SAP announcement (engine locates the primary source) |
| Routine (folded) | Published `next_review_due` 2026-06-22 has lapsed; v0.3.2 base-date routine (2026-03-24 + 365 d) not yet due. Folded because all six dimensions are re-evaluated anyway (see SCOPE). | 2026-09-25 | — |

---

## SCOPE

**Lane:** Interrupt (T1 ×n; T2 on record) with folded routine triggers.
**Mandatory re-evaluation:** R (T1 / T2), plus KEV flag protocol. Because the prior record is a migrated capture with no `sources`, the carry-forward conditions cannot be positively verified for D, I, C; T and V are folded routine scope. **All six dimensions are re-evaluated.** `differential` = `re-evaluated` for v, r, d, i, c, t; `e: null`. This update also resolves the migrated `qa` state (full protocol, all `pass`), records `evaluator_model`, and fills `homepage`, `github` (repository URL per Schema-GithubField-001), `target_version`, `sources` and `tags`.
**Trailing 12-month window for R:** [update date − 12 months] → [update date].
**Enumeration (mandatory, paginated):** The sweep saw >100 advisories at the `per_page` cap; the repository advisories index runs to 14+ pages. Enumerate the full set with pagination from at least two of: GitHub Advisory Database REST (`GET /advisories?ecosystem=npm&affects=n8n&per_page=100&page=N` until an empty page; repeat for `affects=@n8n/task-runner`, `n8n-nodes-base`, `n8n-workflow`, `n8n-core`), repository advisories (`GET /repos/n8n-io/n8n/security-advisories?per_page=100&page=N`), OSV (`POST https://api.osv.dev/v1/query` with `{"package":{"name":"n8n","ecosystem":"npm"}}` following `next_page_token`; repeat for the scoped packages), and the OSV / GitHub web listings as fallback. Record the enumeration method and page count in the Differential. `cve_count_12mo` = exact count of CVE-ID-bearing, product-scoped records published inside the window with basis `exact` only if the enumeration completed; otherwise the count reached with basis `lower_bound` and the reason. CVE-less GHSAs are recorded in the Incident Timeline and considered under T; they are not counted (Attribution-CVE-001); those with published severity ≥ 7.0 are T1 events. De-duplicate GHSA / CVE / OSV aliases and monorepo sub-package aliases before counting (one vulnerability = one count regardless of how many `n8n-*` / `@n8n/*` packages it is filed against).
**KEV flag:** CVE-2025-68613 (2026-03-11) is inside the window — apply the full protocol. Engine checks the KEV JSON for every CVE ID enumerated and for vendorProject/product "n8n" and states any additional entry.
**Attribution / target:** n8n as distributed via npm `n8n` and the monorepo github.com/n8n-io/n8n (including `@n8n/task-runner` and the `n8n-*` workspace packages) — bind `target_version` to the latest release on the update date (dist-tag `latest` 2.40.6 on 2026-09-24; 2.41.3 published 2026-09-25; 1.x maintenance line 1.123.82 — state which line and why). n8n Cloud is the operator's hosted surface: incident effects on it are recorded in the Contextual Analysis and as operator-side evidence under C / I, not scored as the target. Dependency CVEs → `dependency: <vendor/product>`; CVE-less advisories → Incident Timeline and T, not counted.
**CVSS version:** n8n states that CVSS 4.0 is its primary score for all advisories with 3.1 vectors for backwards compatibility. ENGINE.md's reference standard is CVSS v3.1 via NVD: record `max_cvss_12mo` on v3.1 (NIST or CNA) where it exists, note the v4.0 value alongside, and state the choice once in the Differential.
**T5 check (operator / capital structure):** Verify the legal entity (n8n GmbH, Berlin commercial register), the Series C and SAP investment on n8n's own announcements, and whether any controlling stake or operator change is disclosed. Set `operator` to the legal entity, `independence`, `parent_entity`. If a published fact in the record has become incorrect, fire T5 and stage a `KNOWN_FACTS.md` entry candidate in the report's Differential section (Engine ratifies separately; do not edit KNOWN_FACTS.md in the evaluation).

---

## PUBLIC SOURCES TO CONSULT

### Official project
- Homepage: https://n8n.io · Docs: https://docs.n8n.io · Repository: https://github.com/n8n-io/n8n · Releases: https://github.com/n8n-io/n8n/releases · npm: https://www.npmjs.com/package/n8n , https://www.npmjs.com/package/@n8n/task-runner · Blog: https://blog.n8n.io
- Privacy policy / terms / DPA / sub-processors: https://n8n.io/legal/ (engine verifies the current URLs for privacy, terms, DPA, sub-processor list and security page). If a legal page is client-side rendered and cannot be read, flag `[UNVERIFIED]` with the resolving URL and deliver; Tatsuya supplies the body for pre-publication re-scoring (ways-of-working convention).
- Telemetry: engine reads the current telemetry / diagnostics documentation and defaults (`N8N_DIAGNOSTICS_ENABLED` or successor) including endpoints; task-runner sandbox configuration (`N8N_RUNNERS_ENABLED`, `N8N_RUNNERS_MODE`, `N8N_NATIVE_PYTHON_RUNNER`) for the C dimension.
### Security and vulnerability disclosure
- GitHub Security Advisories: https://github.com/n8n-io/n8n/security/advisories · SECURITY.md / security policy: https://github.com/n8n-io/n8n/security/policy · NVD search terms: "n8n", "n8n-io" · OSV: https://osv.dev/list?q=n8n · CISA KEV JSON (cisagov/kev-data mirror acceptable) and the CISA alert for the 2026-03-11 addition
- Third-party: Cyera Research Labs (CVE-2026-21858 "Ni8mare"), Qualys ThreatPROTECT (CVE-2025-68668), national CERT alerts (CSA Singapore, AhnLab ASEC) for disclosure-speed evidence
### Ownership and identity
- n8n GmbH (Berlin commercial register HRB entry via handelsregister.de or a mirror); Series C announcement (2025-10-09) and the SAP strategic investment (May 2026) on n8n's blog / newsroom; SOC 2 / ISO statements on n8n's security or trust page
### Release and activity record (dormancy test)
- https://github.com/n8n-io/n8n/releases — expected active (1,182 npm releases; 2.x line daily cadence)

---

## FIELD SEMANTICS

Per `_template_update.md` and ENGINE.md Differential Evaluation. Specifically for this record: `cve_count_12mo` and `cve_count_basis` as in SCOPE (Enumeration); `max_cvss_12mo` per the CVSS-version rule in SCOPE (expected 10.0 on v3.1 for CVE-2026-21858 if inside the window); apply the −1 count penalty and the 0-point maximum-CVSS band for CVSS 9.0+; `cisa_kev.present: true` with `entries` carrying `cve_id`, `kev_added_date`, `fcec_deadline` for CVE-2025-68613 and any addition; `supply_chain_compromise_12mo` recomputed; `evaluator_model` recorded; `qa` all `pass`; `selection_basis` absent (pre-frame record — do not add); `verdict.<dim>.note` ≤ 80 characters; omit `dormant_since`; `previous_evaluation_date: 2026-03-24`, `previous_score: 35`; `evaluation_type: update`; `differential` all `re-evaluated`, `e: null`; `homepage`, `github`, `target_version` filled; `sources` populated with every URL cited; `tags` populated (lowercase-hyphen; include `tier-<x>`, `cisa-kev`, and the structured disclosure tags `evaluator-coi` / `shared-investor` only if Trigger 2 fires). Replace the captured "12+ CVEs" language and the fragmentary captured lines in the body with the post-window facts; `key_finding` two sentences, ≤ ~630 characters. Evaluation History: three rows (2026-03-13 initial 40 · 2026-03-24 update 35 · this update).

---

## SPECIAL CONSIDERATIONS

### 1. Structural issues criterion
The prior record states "blocklist-based sandbox bypass confirmed across 5+ independent CVEs". The 2026 advisories add expression-engine sandbox escapes (CVE-2025-68613, CVE-2026-25049, GHSA-hx4h-vr3m-45vh), Code-node and Git-node code execution (CVE-2025-68668, CVE-2026-21877, GHSA-rcv6-pvrj-4xcg) and the Task Runner sandbox (GHSA-jjpj-p2wh-qf23). Evaluate "same root cause recurring" on the evidence per class, and record the vendor's structural responses in the window (task-runner isolation, external runner mode, expression-evaluation safeguards in 1.122.0) as positive findings. Record first-report → patch elapsed times per CVE from the advisories and release tags. No motive attribution.

### 2. KEV protocol and score-change explanation
CVE-2025-68613: disclosure 2025-12-19 (GHSA-v98v-ff95-f3cp, fixed 1.122.0) → KEV addition 2026-03-11 (82 days) → FCEB deadline 2026-03-25. Record in the Scorecard KEV line, Incident Timeline, Executive Summary and Contextual Analysis. R will move on the count band and the 9.0+ penalty; if the total moves by ≥5 points, the Executive Summary names the dimensions and evidence. `previous_score: 35` is published alongside.

### 3. Disclosure layer
Triggers 0 / 1 / 3: None expected (independent operator; no set member as parent; standard marketplace / model-provider relations only). Trigger 2: NVentures (NVIDIA's venture arm) participated in the 2025-10-09 Series C as a "corporate investor" per n8n's announcement, in a round led by Accel. NVIDIA is a material Anthropic equity-holder (KNOWN_FACTS.md). Engine applies the material-investor test on the primary source (n8n's announcement and any funding-round filing): ≥ USD 100M position, board seat, or designation as lead or strategic investor. If any element is met, Trigger 2 fires — add the Trigger 2 disclosure paragraph after the mandatory Bias Disclosure paragraph and the tags `evaluator-coi`, `shared-investor` (Stage3-Independence-001); if none is met on the public record, state "None" and note the participation for completeness, as #064 did for Microsoft / GitHub Fund. The SAP investment (May 2026) involves no set member and is recorded under T5 only.

---

## KNOWN FACT CORRECTIONS

None applicable (no n8n entry in `KNOWN_FACTS.md` at drafting). The Anthropic Equity-Holder Records (NVIDIA entry) are the reference for Special Consideration 3.

---

## OUTPUT RULES

Per `_template_update.md` and the #064 two-file delivery convention: (1) `verdict_001_n8n_update.md` — full English report (`Evaluation type: Update`; header lines for Disclosure layer and Evaluator model) with a Differential section (reason no carry-forward was possible; enumeration method and page count; CVSS-version choice; prior-record omissions stated as facts, including any in-window CVE the prior `lower_bound` count did not carry; T5 findings; Trigger 2 determination) and an Evaluation History table (2026-03-13 initial 40 · 2026-03-24 update 35 · this update), followed by the Japanese summary (`評価種別: 更新`, 前回スコア, CISA KEV 該当あり with CVE ID and 登録日) and the score tag line, QA block excluded; (2) `platforms/n8n.md` — complete front matter with every field above set and the body in the #064 structure (title / summary paragraph / Layer 0 Score line / CISA KEV section in English, replacing the captured fragments / Bias Disclosure with the disclosure-layer paragraph(s) / Full Evaluation embedding the report). Schema-driven values (`next_review_due`, YAML anchor expansion) are applied by Operations.

---

## REMINDERS

Public documentation only. Silence = 0. Positive findings alongside risks (maintainer-published advisories with CVSS 4.0 and 3.1 vectors, patched versions declared, task-runner isolation, security policy page, active release cadence). No intent attribution — record dates and elapsed times, not reasons. The prior score is published, not smoothed.

---
# END OF PROMPT
